//create an array and print item inside array

const sampleArray = ['a','b','c','d','e'];
let num=0;
while(num <sampleArray.length){
    // console.log(num,sampleArray[num]);
    // console.log(`${num} - ${sampleArray[num]}`);
    sampleArray[num] = sampleArray[num] +'procodrr'
    num++;
}